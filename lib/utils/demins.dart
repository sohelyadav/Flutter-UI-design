//App dimensions
import 'package:flutter/material.dart';

const kAppPadding = 16.0;
const kAppCornerRadius = 8.0;
const kWidgetPadding = 8.0;

//Wallet view dimensions
const BorderRadiusGeometry kSlidingUpPanelCornerRadius = BorderRadius.only(
  topLeft: Radius.circular(36.0),
  topRight: Radius.circular(36.0),
);

const BorderRadiusGeometry kSlidingUpPanelOpenCornerRadius = BorderRadius.only(
  topLeft: Radius.circular(0.0),
  topRight: Radius.circular(0.0),
);
